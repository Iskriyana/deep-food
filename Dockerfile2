FROM ubuntu:20.04

RUN apt-get update && apt-get install -y \
	git \
	curl \
	ca-certificates \
	python3 \
	python3-pip \
	sudo \
	&& rm -rf /var/lib/apt/lists/*
	
RUN git clone https://github.com/Iskriyana/deep-food.git 
RUN cd ./deep-food

RUN cd /deep-food/ && pip3 install -r requirements_deployment.txt

#This is what makes the cv2 work!

RUN sudo apt update

RUN sudo apt install libgl1-mesa-glx

RUN sudo apt-get install libglib2.0-0


# REMEMEBR THAT YOU NEED TO COPY THE FILE!!!
# Change the web.py
# see if you can download the variables.data 



